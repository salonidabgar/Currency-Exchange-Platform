import { Web3Response } from "@coinbase/wallet-sdk/dist/relay/Web3Response";
declare type ResponseCallback = (response: Web3Response) => void;
export declare class WalletSDKRelayEventManager {
    _nextRequestId: number;
    callbacks: Map<string, ResponseCallback>;
    makeRequestId(): number;
}
export {};
