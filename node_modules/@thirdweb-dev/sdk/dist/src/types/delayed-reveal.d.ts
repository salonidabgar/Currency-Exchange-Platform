import { BigNumber } from "ethers";
import { NFTMetadata } from "@thirdweb-dev/sdk/dist/src/schema/tokens/common";
export interface BatchToReveal {
    batchId: BigNumber;
    batchUri: string;
    placeholderMetadata: NFTMetadata;
}
